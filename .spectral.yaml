extends:
  - spectral:oas
rules:
  oas3-server-not-example.com: off
  info-contact: off
  info-license: off
  # WebhookPayload is intentionally exported for SDK consumption even though
  # the v1.0.0 API does not deliver webhooks itself.
  oas3-unused-component: off
