# Contracts findings
- LOW: SnapshotVerifier.tact:156-160 receive("set_registry") sets self.proposal_registry = sender() (==deployer by require). No message variant carries an Address → registry addr can never be stored; write-once makes it permanent. getProposalRegistry() returns deployer. Informational getter only, no auth path uses it (EligibilityCheckResponse validated by ProposalRegistry independently). Fix: typed message SetProposalRegistry{registry:Address} like TransparencyRegistry.
- LOW-MED: MerchantPaymentHub.tact — no handler credits account_balances from external source. Only creditBalance caller is payment success (337). SetAccountBalance removed in #363. Comment says "funded by settlement flow" but no such handler. Every payment reverts ERROR_INSUFFICIENT_BALANCE. Distinct from #397 (registration). NOTE: fixing = protocol economics decision; FILE only, don't implement contract change.
