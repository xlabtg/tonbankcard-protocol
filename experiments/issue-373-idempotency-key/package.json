{
  "name": "issue-373-idempotency-key",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone reproduction of PC-04 / #373 — generateIdempotencyKey must recurse into nested metadata (canonical serialisation) instead of using a replacer array that drops metadata.* and collides distinct requests.",
  "scripts": {
    "test": "jest"
  },
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.repro.spec.ts"
    ]
  }
}
