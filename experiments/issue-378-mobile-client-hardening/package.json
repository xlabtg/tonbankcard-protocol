{
  "name": "issue-378-mobile-client-hardening",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone reproduction of PC-09 / #378 — mobile client hardening: percent-encode interpolated identifiers in mobile/ request URLs, parse the URL (not a case-sensitive prefix) when enforcing HTTPS in mobile-app, and drop the no-op android:autoVerify from the custom-scheme deep-link filter.",
  "scripts": {
    "test": "jest"
  },
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.repro.spec.ts"
    ]
  }
}
