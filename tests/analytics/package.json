{
  "name": "@tonbankcard/analytics-tests",
  "version": "0.1.0",
  "private": true,
  "description": "ts-jest specs for the F7 Analytics & Reporting CI readiness validator (Issue #142).",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/AnalyticsReadinessValidator.spec.ts"
    ],
    "testTimeout": 60000
  }
}
