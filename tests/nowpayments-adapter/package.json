{
  "name": "@tonbankcard/nowpayments-adapter-tests",
  "version": "0.1.0",
  "private": true,
  "description": "ts-jest regression suite locking the real HMAC-SHA512 IPN signature verification of the NOWPayments adapter (PC-03 / Issue #372).",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.spec.ts"
    ],
    "testTimeout": 60000
  }
}
